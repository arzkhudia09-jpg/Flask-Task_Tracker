# 📝 Flask TODO Web Application

A simple and clean **TODO web application** built using **Flask** and **SQLAlchemy**, implementing full **CRUD (Create, Read, Update, Delete)** functionality with proper HTTP practices.

This project is ideal for learning:

- Flask backend fundamentals
- Database integration with SQLAlchemy
- CRUD operations
- Web form handling
- Deployment-ready Flask structure

---

## 🚀 Features

- ➕ Add new tasks
- 📋 View all tasks
- ✏️ Update existing tasks
- 🗑️ Delete tasks
- 🕒 Automatically track task creation time
- 🔁 Prevent duplicate submissions using **POST → Redirect → GET (PRG pattern)**

---

## 🛠️ Tech Stack

- **Backend:** Flask (Python)
- **Database:** SQLite (SQLAlchemy ORM)
- **Frontend:** HTML + CSS (Jinja2 templating)
- **Server (Production-ready):** Gunicorn (optional for deployment)

---

## 📂 Project Structure
